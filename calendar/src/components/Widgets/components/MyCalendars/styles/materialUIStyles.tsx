import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles((theme) => ({
  root: {
    width: '100%',
    paddingLeft: 30,
    paddingBottom: 30,
  },
  title: {
    fontSize: 17,
    marginBottom: -3,
  },
  pos: {
    marginBottom: -10,
  },
}));
